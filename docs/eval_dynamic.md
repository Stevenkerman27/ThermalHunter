# `eval_dynamic.py`

## 职责

在固定随机种子生成的相同动态飞行场景中，比较随机、定速定滚转和已训练 PPO 策略。

## 契约

每个场景固定风场起始帧、位置、高度和航向；每个策略各自创建环境并使用该场景，避免策略之间共享可变环境状态。随机策略使用独立但固定的策略随机种子。

输出每种策略的平均高度变化、平均总能量高度变化、逐场景 CSV 与比较图。该结果只用于动态 PPO 实验，不与旧稳态表格 Q/DQN 的数值作算法优劣比较。

运行 `python eval.py --dynamic`。
