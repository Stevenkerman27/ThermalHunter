目标：
1. 检查代码当前状态，对当前机器环境状态做适配，确保可以运行
2. 统一设置到config.py文件，确保每次跑训练/评估都是consistent的
3. 检查是否有代码不符合预期的bug
4. 扩充项目功能。保留表格和深度Q两种学习方法和当前有异同的环境设置，确保可以在配置中选择是训练表格Q还是DQN，确保在相同的风场中进行训练。使用(风场rms速度/滑翔机典型速度)的值来决定风场放大系数，先取0.5，风场时间演变速度不变。同时重构代码入口，确保有统一的训练/评估入口代码，确保默认参数可用且可参数指定细节。